FIRECRAWL_API_KEY="fc-..."
INGESTOR_SUPABASE_URL="https://[PROJECT].supabase.co"
INGESTOR_SUPABASE_SERVICE_KEY="eyJh..."

# Gemini auth: Vertex AI (prod, billed via Cloud Billing/ADC) or a plain
# AI Studio API key (staging/local fallback) — see get_gemini_client() in main.py.
GOOGLE_GENAI_USE_VERTEXAI="false"
GOOGLE_CLOUD_PROJECT="alfred-prod-502215"
GOOGLE_CLOUD_LOCATION="global"
GEMINI_API_KEY="AIza..."

# Optional -- crash/error visibility. Empty = Sentry off (local dev default).
SENTRY_DSN=""
ENVIRONMENT="local"
